{
  "name": "Rust",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "..",
    "options": ["--platform=linux/amd64"]
  },
  "remoteUser": "vscode",
  "remoteEnv": {
    "PATH": "/home/vscode/.opencode/bin:${containerEnv:PATH}"
  },
  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  },
  "runArgs": [
    "--platform=linux/amd64"
  ],
  "customizations": {
    "vscode": {
      "settings": {
        "rust-analyzer.cargo.targetDir": "target"
      },
      "extensions": [
        "rust-lang.rust-analyzer",
        "ms-vscode.cpptools"
      ]
    }
  },
  "postCreateCommand": "cargo --version || true; if ! command -v curl >/dev/null 2>&1; then sudo apt-get update && sudo apt-get install -y curl; fi; if [ ! -x \"$HOME/.opencode/bin/opencode\" ]; then curl -fsSL https://opencode.ai/install | bash; fi; \"$HOME/.opencode/bin/opencode\" --help >/dev/null || true"
}